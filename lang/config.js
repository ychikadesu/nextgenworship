// Language system config. Load this before the individual language files.
// Default interface language if the visitor has no saved preference.
window.SONGBOOK_DEFAULT_LANG = "mn";

// Preferred order for the language dropdown. Any loaded language not listed
// here just falls in after these, alphabetically by its own key — so a new
// language file works with zero edits here, this is just for nicer ordering.
window.SONGBOOK_LANG_ORDER = ["mn", "en", "kr", "mn2"];
